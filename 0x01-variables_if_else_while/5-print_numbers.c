#include <stdio.h>

/**
 * main - prints all single digit numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		if (a != 10 && a < 10)
		{
			printf("%d", a);
		}
	printf("\n");
	return (0);
}
